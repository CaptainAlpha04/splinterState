export type SplinterResult = {
  countryId: string;
  newCountryIds: string[];
};

export function evaluateSplintering(): SplinterResult[] {
  // TODO: Implement stability rolls and splinter logic.
  return [];
}
